[project]
name = "app"
version = "0.1.0"
dependencies = [ "fastapi>=0.104.0", "uvicorn>=0.24.0", "starlette>=0.27.0", "pydantic>=2.0.0", "sqlalchemy>=2.0.0", "pandas>=2.0.0", "numpy>=1.24.0" ]
requires-python = "~=3.14.0"
